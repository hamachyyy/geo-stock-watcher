# 定期監視＋通知システムの設計プレイブック

「Webページや外部状態を定期的に見張って、変化したら自分に通知する」——このタイプの
システムを次に作るときの、汎用の設計指針。特定のサイトや言語に依存しない形でまとめる。
（このリポジトリ固有の反省は [POSTMORTEM.md](POSTMORTEM.md)、使い方は [README.md](README.md) を参照）

---

## 0. 最初に決める3つ

| 問い | 例 | なぜ最初か |
|---|---|---|
| **見逃しの許容度** | 「1時間遅れは許す」か「数分でも困る」か | 監視間隔・多重化の要否がここで決まる |
| **誤通知の許容度** | 鳴りすぎと鳴らなさすぎ、どちらが痛いか | 判定の厳しさ・クールダウン設計に直結 |
| **止まったら誰が困るか** | 自分だけ / 顧客 | 死活監視をどこまで作り込むかの基準 |

この3つを曖昧にしたまま作ると、「動いてるつもりで実は数時間止まっていた」に必ず行き着く。

---

## 1. 鉄則：**監視は自分の死を報告できない**

最重要の原則。プロセスが落ちれば「落ちました」の通知も出せない。
**沈黙を検知できるのは外部の第三者だけ。**

### 実装パターン：デッドマンスイッチ
- 監視が正常なら一定間隔で外部サービスに「生存 ping」を送る
- ping が途絶えたら、**その外部サービスが**あなたに通知する
- 無料例: healthchecks.io、Better Uptime、cron 監視系

```
[監視プロセス] --5分ごとに ping--> [外部の死活監視] --途絶えたらメール--> [あなた]
```

### 通知経路は「監視対象と運命を共にしない」こと
- 本通知に push サービス A を使うなら、死活監視の通知は**別経路（メール等）**にする
- A 自体が落ちたら本通知も死活通知も両方沈黙する、では意味がない
- 「独立した2経路」が最低ライン

---

## 2. 実行場所は1つに頼らない（多重化）

単一ホストは単一障害点。理想は**性質の違う2か所**で同じ監視を回す。

| 実行場所 | 長所 | 短所 / 罠 |
|---|---|---|
| **CI（GitHub Actions 等）** | 保守ゼロ・無料枠 | スケジュールが不正確（後述）・実行時間上限 |
| **手元PC（常時起動＋launchd/cron）** | 即時・自由 | 電源/スリープ/回線に依存 |
| **VPS / クラウド無料枠** | 安定・常時 | 初期構築と維持の手間 |

2か所で回すときは**死活監視も場所ごとに分ける**。1つのpingを共有すると
「片方だけ死んでも気づけない」。

---

## 3. CI のスケジュールは信用しない

GitHub Actions の `schedule:` は**要求どおりに発火しない**。この案件の実測値：

- `*/5`（5分ごと）指定に対し、実発火率 **約5%**
- 発火間隔の中央値 **73分**、最大 **231分**、一度は**34時間の完全停止**も観測

一方で **手動トリガ（`workflow_dispatch`）は間引かれない**（数秒でキューされる）。
これを使った回避策：

### 自己連鎖（self-chaining）パターン
1. 1回のジョブ内で**長時間ループ**を回す（CIのジョブ上限-余裕。例: 6h上限なら5.5h）
2. ループ終端で、自分自身を `workflow_dispatch` API で**起こしてから**終了する
3. 新しいジョブが即座に始まり、`schedule` の不正確さを回避できる

**必須のガード（暴走防止）:**
- 連鎖には CI 標準トークンでは不可 → **書き込み権限付きトークンが別途必要**
  （GitHub なら Fine-grained PAT / `Actions: read and write` のみ。`contents` は付けない）
- 「実行がループ長の8割未満なら連鎖しない」条件を入れる。
  即死→即再起動の**無限ループでクレジットを溶かす事故**を防ぐ

### 並行実行の扱い（GitHub の場合）
- `concurrency` グループ + `cancel-in-progress: false` で「実行中1・待機1」に制限
- 新しい発火は**待機中のものを置き換える** → 古い待機は `cancelled` になる
- ⚠️ **この `cancelled` は失敗ではなく正常動作。** 履歴/アラートから除外しないと
  誤検知の嵐になる（実際にやらかした）

---

## 4. トークン・秘密情報の扱い

| 種類 | 置き場所 | 絶対にやらないこと |
|---|---|---|
| API トークン | CIのSecrets / ローカルの gitignore 設定ファイル | チャット・コミット・シェル履歴に残す |
| 通知先ID（topic等） | 同上 | 公開リポジトリに直書き（=実質的な共有秘密） |
| 死活監視のURL | 同上 | 誰でも偽ping可能なので秘密扱い |
| メールアドレス等PII | 同上 | コミットの author に本物を使う（noreply を使う） |

### 運用ルール
- Secrets 登録は**本人が自分の端末で** `gh secret set NAME`（値は echo されない）。
  代理入力させない／させられない前提で設計する
- **コミット前に必ず staged diff を grep** して漏れを機械的に止める：
  ```
  git add -A && git diff --cached | grep -E 'topic_pattern|your_email|token_prefix'
  ```
- 設定は**層で上書き**：`config.json`（公開・デフォルト）
  ← `config.local.json`（gitignore・秘密） ← 環境変数（CI/一時上書き）

### ⚠️ トークンの有効期限は「新たな静かな故障点」
自己連鎖や外部cronに使うトークンには期限がある（PATは最長1年）。
**切れると連鎖が音もなく止まる。** 対策：
- 期限切れ時の失敗（HTTP 401/403）をログに出し、**最終的に死活監視が拾う**設計にしておく
- 期限をカレンダーに登録するか、期限監視自体を作る

---

## 5. 状態の永続化：「変化時だけ書く」の罠

差分検知システムは「前回の状態」を保存する。ここで設計判断が要る：

| 方式 | 長所 | 罠 |
|---|---|---|
| 毎回書く | `last_run` が常に最新＝生存確認に使える | CI だと毎回コミットが出てノイズ |
| **変化時だけ書く** | コミット/書き込みが静か | **ファイル更新時刻＝最終実行時刻ではない**。<br>「20時間更新なし」に見えて実は正常、を招く |

**教訓：** ファイルの mtime や `last_run` を生存確認に使うな。
**生存の一次情報はログ（毎回追記される）か死活pingにする。**
「変化時だけ書く」を採るなら、実行ログは別に毎回残すこと。

---

## 6. 判定ロジックは「誤検知しない」を最優先

通知系の信頼は**最初の誤報で死ぬ**。「1回鳴らなかった」より「1回誤って鳴った」方が
システムへの信頼を大きく削る。

- **多段バリデーション**：単一の文字列一致で判定しない。
  「対象要素の有無」「ページが期待構造か」など複数条件を通す
- **UNKNOWN 状態を作る**：`in_stock` / `sold_out` の二値でなく、
  「ページ構造が想定外＝判定不能」を第三の状態として持つ。
  判定不能を「変化あり」に混ぜない
- **取得失敗（HTTP 000/5xx）は在庫変化ではない**。連続エラー回数を別に数える

---

## 7. アラート疲れを設計で防ぐ

鳴りすぎるシステムは、いずれ**無視される** = 実質停止と同じ。

- **クールダウン**：同じ異常アラートは N 時間に1回まで（`alert_at` を状態に持つ）
- **正常ノイズの除外**：`cancelled` のような設計上正常なイベントをアラートに混ぜない
- **異常の詳細を通知に載せる**：「エラーが3回連続」だけでなく
  「どの対象が・どんなエラーで・何回」まで入れると、開かずに一次判断できる
- **回復通知も出す**：「復活しました」だけでなく「元に戻りました」も。
  状態の全体像が push だけで追える

---

## 8. 観測性（動いてるか自分で見られるか）

- **オンデマンド更新の口を用意する**：定期実行を待たず「今すぐ確認」できる導線。
  - 注意：ローカルHTML（`file://`）は**コマンドを実行できない**。
    ブラウザのボタンから叩きたいなら `127.0.0.1` バインドの小さなHTTPサーバを
    受け口にする（同一オリジンガードを付ける）
- **履歴を残す**：判定の時系列（jsonl等）＋人が読めるHTML化。
  「本当に動いてる？」への即答になる
- **1画面サマリ**：現在の各対象の状態・最終実行・連続エラー数を一望できるように

---

## 9. 選択肢を比較するときの観点（この案件で検討したもの）

| 案 | 初期コスト | 維持 | 安定性 | 備考 |
|---|---|---|---|---|
| CI のみ（schedule） | 最小 | ゼロ | ✕ 不正確 | 単体では穴だらけ |
| CI 自己連鎖 | 中 | ゼロ | ○ | 本案件の主軸。トークン期限だけ注意 |
| CI + 外部cron発火 | 中 | 低 | ○ | 外部にトークンを預ける管理コスト |
| 手元PC 常時 | 低 | 低 | △ | 電源/回線/スリープ依存。多重化の片翼に最適 |
| クラウド無料枠 | 高 | 中 | ◎ | 常時安定だが構築の手間。要件が上がれば移行先 |

**指針：** 「1つで完璧」を狙わず、**性質の違う2つを重ねて穴を塞ぐ**。
本案件は「CI自己連鎖（主）＋手元Mac（副）＋外部死活監視（審判）」の三層で落ち着いた。

---

## 10. 作り終えたら通すチェックリスト

- [ ] 監視が死んだら**外部**から自分に連絡が来るか（デッドマンスイッチ）
- [ ] その通知経路は監視対象・本通知と**独立**しているか
- [ ] 実行場所は2つあるか。死活pingは場所ごとに分けたか
- [ ] CIのスケジュールに依存していないか（連鎖 or 外部発火にしたか）
- [ ] 連鎖の暴走ガード（最短実行時間チェック）は入れたか
- [ ] トークンの**有効期限**を把握し、失効が検知される仕組みはあるか
- [ ] 秘密情報がコミット/チャット/履歴に漏れない仕組み（grepガード）があるか
- [ ] 生存確認をファイルmtimeでなくログ/pingでできるか
- [ ] 誤検知しない判定（多段検証・UNKNOWN・エラーと変化の分離）になっているか
- [ ] アラートにクールダウンと正常ノイズ除外があるか
- [ ] 「今すぐ確認」と履歴閲覧の導線があるか

---

## まとめ：この案件が教えてくれた3行

1. **監視は自分の死を報告できない。** 沈黙を見張る外部の審判を必ず置く。
2. **CIのスケジュールは信用するな。** 間引かれない手動トリガを連鎖させる。
3. **「動いてるつもり」を疑う仕組み**（独立通知・多重化・ログ生存確認）にこそ工数を割く。
   ——監視対象そのものを検知するコードより、こっちのほうが後で効く。

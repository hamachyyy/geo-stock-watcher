# 振り返りと運用リスク

ゲオモバイル在庫ウォッチャーを作った過程で起きた失敗と、この運用が抱えるリスクの記録。
同じ穴に落ちないため、また将来この仕組みが静かに壊れたときに原因を探れるように残す。

作成日: 2026-08-23
更新: 2026-08-24（アウトレット監視の追加）

---

## 1. 失敗したこと

### 1-1. ノートPCに常駐させた（監視できていたのは22%）

最初の実装は Mac の launchd に常駐させる方式だった。MacBook Pro をバッテリー駆動で
使っている環境では、蓋を閉じた瞬間に監視が止まる。

```
観測期間        08/21 16:51 〜 08/23 01:06（32.3時間）
実チェック回数  100 回（5分間隔なら387回のはず）
監視できた時間   7.2時間（22%）
空白だった時間  25.1時間（78%）
最長の空白      10.2時間（08/22 06:33〜16:46）
```

**なぜ起きたか**: 通知の届け先は確認したのに、「Mac が寝ている間はどうするか」を
確認しないまま作り始めた。指摘したのは私ではなく依頼者だった。

**補足**: macOS は省電力のため 15 分おきに DarkWake するが、その間 launchd の
`StartInterval` ジョブは動かない。68 分の空白の中で DarkWake は 8 回起きていたが、
チェックは 0 回だった。さらに launchd は溜まった実行を 1 回にまとめるため、
復帰後に取りこぼし分が追いつくこともない。「たまに起きるから多少は拾える」は成り立たない。

**教訓**: 監視系を作るときは、まず「いつ動かないか」を洗い出す。

### 1-2. 実行場所を検証前に推薦した

Cloudflare Workers を推薦し、依頼者がそれを選んだ**後**に、このサイトが HTTP/2 を
必須としていることが分かった。Workers は送信の HTTP バージョンもヘッダ順も制御できない
ため、そもそも成立しない可能性が高い。選び直してもらうことになった。

| リクエスト | 結果 |
|---|---|
| HTTP/2 ＋ Chrome 相当のヘッダ一式 | 200 |
| HTTP/2 ＋ User-Agent だけ | 403 |
| HTTP/1.1 ＋ Chrome 相当のヘッダ一式 | 403 |
| HTTP/1.1 ＋ Firefox UA | 403 |
| HTTP/1.1 ＋ sec-ch-ua あり | 403 |
| Python `urllib`（ヘッダを揃えても HTTP/1.1） | 403 |

**なぜ起きたか**: 「curl で 200 が返る」ところまでしか確認せず、**何が 200 の決め手か**を
切り分けないまま実行場所を論じた。決め手が HTTP/2 だと分かっていれば、
最初から「HTTPの中身を自分で決められる環境」に絞れた。

**教訓**: 制約が選択肢を決めるなら、選択肢を出す前に制約を測る。

### 1-3. 在庫判定の目印を、描画後の文字列から取った

最初の実装は「`機能・仕様一覧` がページにあること」を正常なページの目印にしていた。
これはブラウザで描画された後のテキストから拾ったもので、生の HTML には存在しない。
初回実行で両機種とも「判定不能」になった。

**教訓**: HTML を直接読む処理の目印は、必ず生の HTML から取る。

### 1-4. 同じ状態ファイルを2者が書いた

`state.json` を手元の常駐と GitHub Actions の両方が書き換えていた。デプロイ直後に
push が弾かれ、マージ衝突になった。手元を `state.local.json` に分離して解消。

**なぜ起きたか**: 実行場所を追加したとき、書き込み先が競合することを考えなかった。

**教訓**: 実行主体を増やすときは、書き込み先ごとに所有者を決める。

### 1-5. タイムゾーンの取り違え（9時間ずれた履歴を出した）

GitHub のランナーは UTC、手元の Mac は JST で時刻を記録していた。同じ書式なので
統合時に区別されず、履歴HTMLの在庫区間が**16時間57分のところ7時間57分**と表示された。
しかもこの誤った履歴は依頼者に一度渡っている。

```
CI が書いた  since = 2026-08-23 07:50:18  ← UTC
Mac が書いた since = 2026-08-23 19:04:59  ← JST
```

対処はワークフローへの `TZ: Asia/Tokyo` 設定と、既存の UTC 値の補正
（その版を書いたコミットの時刻と突き合わせ、コミット直前2時間以内に書かれた値だけを
UTC とみなす。持ち越された古い値は手元由来なので触らない）。

**教訓**: 複数の場所で時刻を記録するなら、記録側で基準を揃えるかオフセットを持たせる。
naive な日時文字列を別の場所からの値と混ぜない。

### 1-6. cron の設定値を実測値だと思い込んだ

`*/5` と書いたので「5分ごと」と説明していた。実際に測ったのは約18時間後。

```
スケジュール実行  15 回 / 16.9 時間（5分間隔なら203回のはず）
実行間隔          最短 21分 / 中央値 60分 / 最長 3時間30分
```

前回 SE2 は 49 分で売り切れている。中央値 60 分では取りこぼす。

**なぜ起きたか**: 設定を書いた時点で「そう動く」と説明してしまい、実測しなかった。
GitHub Actions の定期実行が間引かれるのは広く知られた挙動で、事前に測るべきだった。

**教訓**: スケジューラの設定値は希望であって実測値ではない。

### 1-7. 方式を変えたのに表示を直さなかった

内部ループ方式（1回の実行を50分回して2分ごとにチェック）に変えた後も、履歴HTMLは
「1実行＝1行」のままだった。50分間に25回チェックしても1行しか出ず、実態を
過少に見せていた。さらに実行中の回を表示から除外していたため、ループ中は GitHub の
最新行が最大50分間現れない状態でもあった。

**教訓**: 動作を変えたら、それを映す画面も同時に直す。

### 1-8. アウトレット一覧ページの商品区画を1種類だと決めてかかった

`/outlet/` を実装するとき、最初に確認した内容（訳アリ品・状態C、22機種29バリアント）
だけを見てページ全体を把握したと判断し、パーサーを実装しかけた。依頼者が
「Galaxy A25 5G も含んでいるか」と聞かなければ気づかないまま、そのままだった。

調べ直すと、このページには構造の異なる2種類の商品区画が同居していた。

| 区画 | 容量分け | 在庫マーカーの位置 |
|---|---|---|
| 訳アリ品（状態C） | あり（1機種で複数バリアント） | 容量ごとの `<dd>` の中 |
| 中古未使用品（Galaxy A25 5G） | なし | `production_mode` 直下に1つだけ |

さらに実装を進める中で、両区画を `<!-- 機種名 --> `というHTMLコメントの有無や
タブの物理的な位置で見分けようとしていたが、これはタブ構造が変われば壊れる
脆い判定だった。最終的には各区画の中身（`price_box_problem` というクラスが
あるかどうか）で判定する方式に直し、タブの位置に依存しない形にした。

**教訓**: 「同じページに見える一覧」でも、区画ごとに構造が違う可能性を疑う。
一部だけ見て「これで全部」と判断せず、ページ全体をコードで数え上げて
確認する（今回は `<section class="item_wrapper">` の総数を数え、
分類できない区画が残っていないか検算した）。

### 1-9. 通底する問題

1-2、1-5、1-6、1-7 は同じ形をしている。**測る前に説明した**。
「5分ごとに動きます」「Workers が最適です」「履歴はこうです」と述べた時点で、
その裏づけとなる実測がなかった。動くものは作れていたが、その説明が実態より
先に進んでいた。

1-8 はこれとは別の形の失敗で、**確認した範囲を、確認していない範囲まで
含めて語った**。22機種29バリアントを検証した時点で「アウトレットの監視を
実装できた」と言おうとしていたが、実際には「訳アリ品カテゴリだけ」を
確認したにすぎなかった。部分的な確認と全体の確認を区別できていなかった。

---

## 2. この運用のリスク

### 2-1. 静かに死ぬ経路がある（最も重い）

現在の健全性チェックは「6回連続で判定不能なら警告通知」だが、これは
**ワークフローが実行されて初めて動く**。つまり次のケースでは何の通知も出ない。

| 壊れ方 | 検知できるか |
|---|---|
| ページ構造が変わって判定不能 | ○ 6回連続で警告 |
| サイトが 403 を返すようになった | ○ 6回連続で警告 |
| **GitHub が定期実行を止めた** | **× 何も起きない** |
| **ワークフローが無効化された** | **× 何も起きない** |
| **リポジトリが凍結された** | **× 何も起きない** |

沈黙が「在庫切れが続いている」なのか「監視が死んでいる」なのか区別できない。
定期的に履歴HTMLのカバー率を見るか、外部から死活監視する必要がある。

### 2-2. GitHub の利用規約上グレー

Actions を長時間ループさせ続ける運用は、GitHub が想定する用途
（ソフトウェアのビルド・テスト・デプロイ）から外れる。停止させられる可能性がある。
その場合は Google Cloud Run + Cloud Scheduler などへの移行が必要。

### 2-3. 60日ルール

リポジトリに60日間動きがないと定期実行が自動停止する。警告メールが届くので、
その際は手動でコミットするか Actions 画面で再有効化する。
CI が状態変化時に打つコミットがこれを回避できるかは未確認。

### 2-4. ボット判定は明日変わりうる

現在通っているのは「HTTP/2 ＋ Chrome 相当のヘッダ一式」という**文書化されていない
組み合わせ**にすぎない。Akamai の設定変更、GitHub ランナーIPの評価変化、
ヘッダ要件の変更のいずれでも 403 に戻る。その場合は 2-1 の警告が出る（実行されていれば）。

### 2-5. 通知経路の弱さ

- ntfy.sh のトピック名は**知っている人なら誰でも購読でき、誰でも投稿できる**。
  トピック名はリポジトリに入れていないが、秘密が漏れれば偽の通知を送られうる
- 無料の公開サービスであり、配信保証はない
- **依頼者がスマホで購読しているかは未検証**。送信成功のログは残るが、届いたかは別問題

### 2-6. 公開リポジトリであること

`state.json` のコミット履歴から、どの機種をいつから見張っているかが公開されている。
通知先トピックは Secrets にあるが、監視していること自体は隠れていない。

### 2-7. 二重通知

手元の Mac 常駐と GitHub Actions が独立に状態を持つため、在庫復活時に通知が2通届く。
`./uninstall.sh` で Mac 側を止めれば1通になるが、履歴HTMLの Mac 側の記録も止まる。

### 2-8. 監視先サイトへの負荷

2分間隔で2ページを叩く。監視できている時間帯では 1 分あたり約1リクエスト。
相手の許容範囲を明示的に確認したわけではない。間隔を詰めるほど検知は速くなるが、
迷惑行為との境界に近づく。

### 2-9. 履歴の一部は推定である

「最近のチェック」の GitHub 行は、実行記録と状態の推移から**組み立てた**もので、
実際のログを読んだものではない。CI は状態が変わらないと記録を残さないため、
「コミットが無い間の実行は直前と同じ状態を見ていたはず」という推論に基づく。
推論の前提（判定不能もエラー回数の変化としてコミットされる）が崩れると、
表示も崩れる。

また区間の開始時刻は**検知した時刻**であって、実際に在庫が復活した時刻ではない。
監視が途切れていた間に復活していた場合、実際はそれより早い。

### 2-10. 検知が速くても間に合うとは限らない

これは仕組みの限界。前回 SE2 は49分で売り切れた。通知が届いてから
機種・容量・色を選んで手続きを終えるまで人間の操作が要る。
通知は「気づける可能性を上げる」ものであって、購入を保証しない。

### 2-11. アウトレット監視は個別ページ監視より壊れやすい

個別の商品ページは「マーカー1個の有無」を見るだけだったが、アウトレット一覧は
1ページに約30バリアントの状態を、区画の中身から分類しながら抜き出している。
壊れ方の種類が増えている。

- **マーカーのクラス名（`-soldout`）が変わると、全バリアントが同時に
  「在庫あり」に見える**。実際に検証で再現した。対策として、既知の在庫切れの
  7割以上が同時に在庫ありへ変化したら更新を保留するガードを入れているが、
  これは「本当に多数の機種が同時に再入荷することはない」という経験則に基づく
  ヒューリスティックであり、絶対的な保証ではない。閾値未満の変化（例: 30件中5件が
  同時に化ける）はすり抜ける
- **区画数・バリアント数の範囲チェック**（15〜60件）も同様にヒューリスティック。
  サイト側が本当に商品を大幅に増減させた場合、正常な変化を異常と誤判定する可能性がある
- **通知をタップしても個別商品には飛べない**。アウトレットの購入はCSRFトークン付き
  フォームのPOST送信のため、深いリンクを作れない。一覧ページを開いて手動で探す必要がある
- Galaxy A25 5G（中古未使用品）は現時点でこのカテゴリの唯一の商品であり、
  このカテゴリ用のパーサー（`_parse_outlet_new_unused_section`）は**1商品でしか
  検証できていない**。同カテゴリに2商品目が追加されたときに初めて、
  複数商品を正しく扱えるかが試される

---

## 3. いま確認すべきこと

- [ ] スマホに ntfy の通知が実際に届くか（`gh workflow run watch.yml -f mode=test-notify`）
- [ ] ループの連鎖が成立しているか（履歴HTMLのカバー率。数時間後に判断できる）
- [ ] 二重通知を許容するか、Mac 側を止めるか
